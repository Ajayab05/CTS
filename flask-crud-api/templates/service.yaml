apiVersion: v1
kind: Service
metadata:
  name: {{ include "flask-crud-api.fullname" . }}
  labels:
    {{- include "flask-crud-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flask-crud-api.selectorLabels" . | nindent 4 }}
